Generator tests are separated into `org.ikasan.studio.testing.engine` and
`org.ikasan.studio.testing.packs`. See `docs/TestingEngineAndMetaPacks.md` for
ownership, fixtures, suite selection and the limits of generated-output tests.

`TestUtils` remains here as the resource reader used by existing model/JSON tests.
