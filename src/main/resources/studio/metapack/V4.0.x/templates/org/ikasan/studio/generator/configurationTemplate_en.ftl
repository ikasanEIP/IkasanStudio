package ${studioPackageTag};

/**
* The main responsibility of the configuration bean is to encapsulate the application properties used to support a component.
*
* This is an auto generated stub. The user is expected to fill in the details of the conversion below.
* This stub will not be over-written unless the overwrite checkbox is explicitly selected.
*/

import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.boot.context.properties.EnableConfigurationProperties;
import org.springframework.stereotype.Component;

@org.springframework.stereotype.Component
@org.springframework.boot.context.properties.EnableConfigurationProperties
@org.springframework.boot.context.properties.ConfigurationProperties(prefix="${prefix}")
public class ${className}
{
//@todo user to declare the properties with getter and setters
//@todo user to define the properties in a properties file with prefix ${prefix}
}